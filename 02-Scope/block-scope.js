{ 
    let a = 10;
    let b = 20;

    console.log("Inside block: ", a, b);
}
//console.log(a); //Error 
//console.log(b); //Error as a & b are declared inside a block not accessible outside the block 